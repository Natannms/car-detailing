# Netlify: Next.js — o plugin gerencia rewrites/redirects para o App Router funcionar corretamente.
# https://docs.netlify.com/integrations/frameworks/next-js/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
